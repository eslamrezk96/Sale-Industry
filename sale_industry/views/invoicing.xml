<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sale.list">
      <field name="name">sale list</field>
      <field name="model">sale.sale</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sale.action_window">
      <field name="name">sale window</field>
      <field name="res_model">sale.sale</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sale.action_server">
      <field name="name">sale server</field>
      <field name="model_id" ref="model_sale_sale"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sale" id="sale.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sale.menu_1" parent="sale.menu_root"/>
    <menuitem name="Menu 2" id="sale.menu_2" parent="sale.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sale.menu_1_list" parent="sale.menu_1"
              action="sale.action_window"/>
    <menuitem name="Server to list" id="sale" parent="sale.menu_2"
              action="sale.action_server"/>
    -->
  </data>
</odoo>